{
  "name": "myapp",
  "private": "true",
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --outdir=app/assets/builds"
  },
  "dependencies": {
    "esbuild": "^0.20.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
